// Frontend-only type definitions
// These mirror Prisma enums but don't require @prisma/client dependency

export enum UserRole {
  ADMIN = 'ADMIN',
  SALESPERSON = 'SALESPERSON',
  ACCOUNT_MANAGER = 'ACCOUNT_MANAGER',
  RECRUITER = 'RECRUITER',
  HR = 'HR',
  EMPLOYEE = 'EMPLOYEE',
}

